完成摘要
- app/poi/[id].tsx: POI 详情页路由，支持 scenic/heritage/museum 三类，展示结构化文化信息与下拉刷新
- lib/poi/poiQueries.ts: queryPoiDetail() 按 id 查询 POI 完整字段，queryPoiVersion() 查询版本用于缓存失效判定

EARS 覆盖矩阵
- EARS-1 → app/poi/[id].tsx：展示结构化文化信息（基础信息、类型特有详情、storyline 占位、图文）
- EARS-2 → app/poi/[id].tsx:onRefresh + queryPoiVersion：下拉刷新重新拉取，版本更新后展示最新内容

验证
- npm run typecheck：通过，零错误
- npm run lint：24 warnings (既有)，零新增错误
