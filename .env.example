# 复制为 `.env` 后填写（勿提交 `.env`）。Supabase 控制台：Authentication → URL configuration 中追加 Redirect：`jike://auth/callback`
# 可选：项目 ref（`xxx.supabase.co` 中的 `xxx`）；供 `scripts/gen-mcp-apply-json.cjs` 等在无 URL 时解析 project_id。
SUPABASE_PROJECT_REF=
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_AMAP_KEY=
